# v0.30.0 GITHUB REBUILD

## 追加

- 灰鳴兄弟（兄と弟で一人のプレイアブルキャラクター）
- 3キャラクター × 5枚の固有カード
- キャラ片を消費する固有カード解放画面
- 灰鳴兄弟の交互使用カウンター「👣足跡」
- GitHub Actionsによるスモークテストと静的ビルド

## 修正

- `build_trust` が付与する `stageBuffs.trust` をターン開始時に適用
- 固有カードを通常カード工房のランダム生成対象から除外
- 報酬候補へ出る固有カードを、選択中キャラの解放済みカードだけに制限
- セーブデータに `unlockedCharacterCards` とschema 30を追加

## 互換性

- v0.27.8までのセーブは読み込み時にバックアップされ、自動移行されます。
- 不明なカードやレリックを即時削除しない既存の隔離処理を維持します。
